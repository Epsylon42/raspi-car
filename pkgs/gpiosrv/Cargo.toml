[package]
name = "gpiosrv"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = { version = "0.5.1", features = ["serde_json"] }
rocket_ws = "0.1"
rust-pigpio = "0.2.0"
serde = { version = "1.0.219", features = ["serde_derive"] }
serde_json = "1.0.140"
